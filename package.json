{
  "name": "offset-netlify",
  "version": "2.0.0",
  "private": true,
  "description": "OFFSET — minimal online newspaper for Netlify. Articles are Markdown files.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs"
  }
}
